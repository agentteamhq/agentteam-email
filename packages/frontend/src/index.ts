import { startFrontendServer } from './server'

export { startFrontendServer } from './server'

// eslint-disable-next-line no-restricted-syntax -- Production package entrypoint starts the HTTP server.
startFrontendServer()
